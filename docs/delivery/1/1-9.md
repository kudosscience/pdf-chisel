# 1-9 Rename application from "PDF Editor" to "PDF Chisel"
[Back to task list](./tasks.md)

## Description
Rename all user-visible and internal references from "PDF Editor" to "PDF Chisel" across the codebase. This includes the window title, constants, package.json product name, build scripts, README, and documentation. The GitHub repo has already been renamed to `pdf-chisel`.

## Status History
| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2026-02-25 12:00:00 | Created | N/A | Proposed | Task file created | AI_Agent |
| 2026-02-25 12:00:00 | Status Change | Proposed | Agreed | User chose name "PDF Chisel" | User |
| 2026-02-25 12:00:00 | Status Change | Agreed | InProgress | Starting rename | AI_Agent |
| 2026-02-25 12:10:00 | Status Change | InProgress | Review | All references renamed, build passes | AI_Agent |

## Requirements
- All user-visible strings must show "PDF Chisel" instead of "PDF Editor".
- Package name must be `pdf-chisel`.
- Build output filenames must reflect "PDF Chisel" / "PDF.Chisel".
- README download links and descriptions must use the new name.
- No functional regressions.

## Implementation Plan
1. Update `APP_NAME` in `src/shared/constants.ts` (and `.js`, `.d.ts`).
2. Update `package.json` — name, productName, description, publisherName, shortcutName.
3. Update `package-lock.json` — name fields.
4. Update `src/main/index.ts` — window title.
5. Update `src/renderer/index.html` — `<title>`.
6. Update `src/renderer/app.ts` — all title bar references.
7. Update `scripts/build-msi.js` — exe, name, manufacturer.
8. Update `README.md` — title, description, download links.
9. Update `docs/delivery/backlog.md` — header.
10. Update test files referencing the old name.

## Test Plan
- TypeScript compilation passes without errors.
- App launches with "PDF Chisel" in the title bar.
- Grep for "PDF Editor" returns zero matches (excluding git history).

## Verification
- TypeScript compilation passes (main + renderer): OK
- No remaining \"PDF Editor\" in source (only in task docs and generic descriptions): OK

## Files Modified
- src/shared/constants.ts, constants.js, constants.d.ts
- src/main/index.ts
- src/renderer/index.html
- src/renderer/app.ts
- package.json
- package-lock.json
- scripts/build-msi.js
- scripts/generate-fixtures.js
- test/e2e/editing.test.ts
- README.md
- docs/delivery/backlog.md
- docs/delivery/1/tasks.md
